{
  "name": "checkers-api",
  "version": "1.0.0",
  "description": "api for checkers app.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "cd .. && cd checkers-client @@ npm run start",
    "start": "nodemon index.js",
    "dev": "concurrently \"npm run start\" \"npm run client\""
  },
  "author": "Nicholas Prendergast",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^6.0.0",
    "nodemon": "^2.0.7"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "socket.io": "^3.1.1"
  }
}
